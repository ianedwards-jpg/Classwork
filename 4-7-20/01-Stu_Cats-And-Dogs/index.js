const dog = {
    raining: true,
    noise: "woof",
    makeNoise: function() {
        if (this.raining === true) {
            console.log(this.noise)
        }
    }
}

const cat = {
    raining : false, 
    noise: "Meow", 
    makeNoise: function () {
        if (this.raining === true) {
            
        }
    }
}